[package]
name = "ising-verifier-final"
version = "0.1.0"
edition = "2021"

[dependencies]
halo2_proofs = { version = "0.3", features = ["dev-graph"] }
pasta_curves = "0.5"
ff = "0.13"
rand_core = "0.6"

[dev-dependencies]
criterion = "0.5"
proptest = "1.4"
